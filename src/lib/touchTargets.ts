/** Shared with Button `size="touch"` — use on native buttons/links when needed. */
export const touchButtonClass = "min-h-16 h-16 sm:min-h-14 sm:h-14";

/** Hero recipe actions (start cooking, preview). */
export const touchProminentButtonClass = "min-h-[5.5rem] h-auto py-5 sm:py-6";
